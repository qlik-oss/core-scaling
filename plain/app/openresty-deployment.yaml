apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: openresty
  labels:
    app: openresty
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: openresty
    spec:
      containers:
      - name: openresty
        image: qlikea/qliktive-custom-analytics-openresty:master-392
        imagePullPolicy: IfNotPresent
        env:
        - name: ERROR_LEVEL
          value: debug
        - name: SESSION_COOKIE_NAME
          value: custom-analytics
        - name: QIX_SESSION_HOST
          value: qix-session
        - name: QIX_SESSION_PORT
          value: "9455"
        ports:
        - containerPort: 443
      imagePullSecrets:
      - name: dockerhub
